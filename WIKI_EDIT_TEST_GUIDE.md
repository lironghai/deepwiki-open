# Wiki编辑功能测试指南

## 功能概述

已实现Wiki页面编辑功能，用户可以手动编辑wiki页面的标题和内容。

## 测试步骤

### 1. 启动服务

**后端（FastAPI）：**
```bash
cd api
python -m api.main
```
后端应运行在 `http://localhost:8001`

**前端（Next.js）：**
```bash
npm run dev
# 或
yarn dev
```
前端应运行在 `http://localhost:29006`

### 2. 访问Wiki页面

1. 打开浏览器访问 `http://localhost:29006`
2. 输入一个已存在的仓库信息（owner/repo）
3. 等待wiki生成完成
4. 选择一个wiki页面查看

### 3. 测试编辑功能

#### 3.1 进入编辑模式
- 在wiki页面标题旁边，点击"编辑"按钮
- 应该看到编辑器界面，包含：
  - 编辑/预览/分屏模式切换按钮
  - 标题输入框
  - 内容编辑区域（Markdown）
  - 保存/取消按钮

#### 3.2 测试编辑模式切换
- **编辑模式**：只显示Markdown编辑框
- **预览模式**：只显示渲染后的内容
- **分屏模式**：左侧编辑，右侧预览

#### 3.3 测试内容编辑
1. 修改标题
2. 修改内容（Markdown格式）
3. 在预览模式下查看效果
4. 点击"保存"按钮

#### 3.4 测试保存功能
- 保存后应该：
  - 显示成功提示（或静默更新）
  - 退出编辑模式
  - 页面内容立即更新
  - 刷新页面后内容仍然保留

#### 3.5 测试取消功能
1. 进入编辑模式
2. 修改内容
3. 点击"取消"按钮
4. 应该提示"有未保存的更改"
5. 确认后退出编辑模式，内容恢复原样

#### 3.6 测试页面切换
1. 在编辑模式下
2. 点击左侧导航切换到其他页面
3. 应该自动退出编辑模式

### 4. 测试缓存一致性

#### 4.1 并发测试（可选）
- 打开两个浏览器标签页
- 同时编辑同一个页面
- 验证后保存的更改不会丢失

#### 4.2 服务器重启测试
1. 编辑并保存一个页面
2. 重启后端服务器
3. 刷新前端页面
4. 验证编辑的内容仍然存在

### 5. 错误处理测试

#### 5.1 网络错误
- 断开网络连接
- 尝试保存
- 应该显示错误提示

#### 5.2 无效数据
- 尝试保存空标题（应该允许）
- 尝试保存空内容（应该允许）

## 预期行为

### ✅ 正常情况
- 编辑按钮可见且可点击
- 编辑器界面正常显示
- 编辑/预览/分屏模式切换正常
- 保存后内容立即更新
- 取消时提示未保存更改
- 切换页面时自动退出编辑模式

### ⚠️ 错误情况
- 网络错误时显示友好提示
- 保存失败时显示错误信息
- 临时文件在错误时被清理

## 技术实现细节

### 后端
- **API端点**：`PATCH /api/wiki_cache/page`
- **缓存一致性**：使用threading.Lock + 临时文件+原子重命名
- **文件位置**：`~/.adalflow/wikicache/deepwiki_cache_{type}_{owner}_{repo}_{language}.json`

### 前端
- **API路由**：`/api/wiki/page` (Next.js代理)
- **组件**：`WikiEditor.tsx`
- **状态管理**：React state (isEditing, editingPageId, isSaving)

## 已知限制

1. 不支持版本历史/撤销功能
2. 不支持协作编辑（多人同时编辑）
3. 编辑时没有自动保存
4. 没有编辑权限控制（所有用户都可以编辑）

## 故障排查

### 问题：编辑按钮不显示
- 检查是否有当前页面（currentPageId存在）
- 检查generatedPages中是否有该页面

### 问题：保存失败
- 检查后端服务是否运行
- 检查网络连接
- 查看浏览器控制台错误
- 查看后端日志

### 问题：保存后内容未更新
- 检查前端状态更新逻辑
- 检查API响应是否成功
- 刷新页面验证缓存是否保存

### 问题：缓存文件损坏
- 删除缓存文件重新生成
- 检查文件权限
- 检查磁盘空间

## 下一步改进建议

1. 添加编辑权限控制
2. 添加版本历史功能
3. 添加自动保存功能
4. 添加协作编辑支持
5. 添加编辑冲突检测和解决机制
